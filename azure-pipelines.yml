trigger:
  - main

pool:
  vmImage: ubuntu-latest
  

stages:
  - stage:
    jobs:
      - job:
        steps:
            - task: trivy@1
              inputs:
                path: .
            
            - task: Bash@3
              inputs:
                targetType: inline
                script: |
                  'trivy image nginx'