trigger:
  - main

pool:
  vmImage: ubuntu-latest
  

stages:
  - stage:
    jobs:
      - job:
        steps:
            - task: trivy@1
              inputs:
                path: .
            
            - task: Bash@3
              inputs:
                script: |
                  trivy image nginx